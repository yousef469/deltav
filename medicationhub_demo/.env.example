# MedicationHub Standalone Demo Environment Config

# URL of your Python backend (running server.py)
VITE_API_URL=http://localhost:8000

# Set to true to skip API calls and always use demo mock data
VITE_USE_MOCK=false

# Note: Ensure the backend has GEMINI_API_KEY configured for Clinical Analysis.
# Voice recording requires HTTPS or localhost in modern browsers.
